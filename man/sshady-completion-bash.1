.nh
.TH "SSHADY" "1" "Jul 2026" "sshady dev" "sshady Manual"

.SH NAME
sshady-completion-bash - Generate the autocompletion script for bash


.SH SYNOPSIS
\fBsshady completion bash\fP


.SH DESCRIPTION
Generate the autocompletion script for the bash shell.

.PP
This script depends on the 'bash-completion' package.
If it is not installed already, you can install it via your OS's package manager.

.PP
To load completions in your current shell session:

.EX
source <(sshady completion bash)
.EE

.PP
To load completions for every new session, execute once:

.SS Linux:
.EX
sshady completion bash > /etc/bash_completion.d/sshady
.EE

.SS macOS:
.EX
sshady completion bash > $(brew --prefix)/etc/bash_completion.d/sshady
.EE

.PP
You will need to start a new shell for this setup to take effect.


.SH OPTIONS
\fB-h\fP, \fB--help\fP[=false]
	help for bash

.PP
\fB--no-descriptions\fP[=false]
	disable completion descriptions


.SH SEE ALSO
\fBsshady-completion(1)\fP


.SH HISTORY
25-Jul-2026 Auto generated by spf13/cobra
